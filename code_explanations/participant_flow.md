# `participant_flow.py`

This script reconstructs manuscript Figure 1 directly from the strict processed
episodes and the fixed patient-level partition. It reports total episodes and
patients, exclusions for unavailable outcome, outcome counts, date range, and
development/test episode and patient counts.

The generated PDF and PNG are written under `results_strict/figures/` and are
git-ignored.
